1) we try to register the tenant wiht multiple Church have same + 6-digit-ID
    but here issue is while creating some issue in app and it get register in DB level
    id agent they try to register again same details, application considered as a register different Church with same name then creat enew one with same + 6-digit-ID

    need to fix this

2) maintain user ID for each user 

3) update the tenant slug don’t use _ in tenant name use - in for  
    from example-church_936800 to example-church-936800
    in db schemas sa well

4) in DB phone number and mail ID should be unique no duplicate